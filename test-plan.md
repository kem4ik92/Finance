# Test Plan — Manat (учёт финансов в TMT)

**URL**: https://dist-tgjbvjlo.devinapps.com
**Scope**: первая end-to-end проверка, что веб-приложение делает то, что просил Kemal: учёт доходов/расходов в туркменских манатах + цель накопления + аналитика + экспорт.
**Storage**: всё в `localStorage` (ключ `finance-tracker-data-v1`), нет бэкенда, нет авторизации.

Before testing: очистить storage в браузере (через DevTools → Application → Local Storage → Clear) чтобы стартовать с чистыми дефолтами (3 счёта: Наличные, Карта, Сбережения; баланс 0,00 м.).

## Primary flow: ежедневное использование (доход → расход → цель → аналитика → экспорт → перезагрузка)

Каждый шаг имеет конкретное ожидаемое значение. Если реализация сломана — шаги выдадут другой результат, а не тот же.

### 1. Стартовое состояние — дашборд пустой, валюта именно TMT
- Action: открыть URL, дождаться загрузки.
- Assert: заголовок вкладки содержит `Manat`.
- Assert: карточка «Общий баланс» показывает ровно `0,00 м.` (не `0,00 ₽`, не `0.00`, не `0,00 TMT`) — проверяет ru-RU локаль + суффикс `м.` из `src/lib/currency.ts`.
- Assert: блок «Последние операции» показывает empty-state «Ещё нет операций».
- **Если сломано**: формат «0.00 $» или «0,00 TMT» вместо «0,00 м.» → fail (валюта не туркменский манат).

### 2. Добавить доход 5 000 TMT
- Action: нажать кнопку `+ Добавить` в правом верхнем углу → в модалке выбрать таб `Доход` → ввести `5000` → Счёт = `Карта` → Категория = `Зарплата` → Сохранить.
- Assert: модалка закрывается.
- Assert: «Общий баланс» стал `5 000,00 м.` (пробел как разделитель тысяч — ru-RU).
- Assert: «Доходы за месяц» = `5 000,00 м.`, «Расходы за месяц» = `0,00 м.`, «Баланс за месяц» = `+5 000,00 м.` (со знаком `+`).
- Assert: в «Последние операции» появилась запись «Зарплата» с суммой `+5 000,00 м.` зелёного цвета.
- **Если сломано**: баланс остался 0 (транзакция не сохранилась) ИЛИ сумма в другой валюте ИЛИ без знака «+» для дохода.

### 3. Добавить расход 350 TMT
- Action: `+ Добавить` → таб `Расход` → `350` → Счёт = `Карта` → Категория = `Продукты` → комментарий «Тест» → Сохранить.
- Assert: «Общий баланс» = `4 650,00 м.` (5000 − 350).
- Assert: «Расходы за месяц» = `350,00 м.`, «Баланс за месяц» = `+4 650,00 м.`.
- Assert: в «Расходы по категориям» (pie-chart) появился сегмент «Продукты».
- **Если сломано**: общий баланс = 5 350 (сложение вместо вычитания), или круговая диаграмма пустая.

### 4. Цель накопления — создание и пополнение
- Action: в сайдбаре кликнуть «Цели» → `+ Новая цель` → название `Новый ноутбук`, целевая сумма `10000`, уже накоплено `0`, Сохранить.
- Assert: карточка цели появилась, текст «0,00 м. из 10 000,00 м.», прогресс-бар 0 %.
- Action: в карточке нажать `+ Пополнить` → сумма `2500`, чекбокс «Списать со счёта как расход» = true, счёт = Сбережения, нажать «Пополнить».
- Assert: в карточке цели теперь `2 500,00 м. из 10 000,00 м.`, прогресс-бар 25 % (численно «25%» или строка «25%» в подписи).
- Assert: в сайдбаре «Общий баланс» = `2 150,00 м.` (4650 − 2500).
- **Если сломано**: прогресс-бар не меняется (contributeGoal не сработал) ИЛИ общий баланс не уменьшился (списание не записалось как операция).

### 5. Аналитика — графики рендерятся
- Action: в сайдбаре «Аналитика».
- Assert: виден AreaChart «Динамика доходов и расходов» с точкой за текущий месяц — линия «Доходы» выше линии «Расходы».
- Assert: виден LineChart «Чистый баланс по месяцам».
- Assert: в «Структура за период» → блок «Расходы» показывает две строки: `Продукты — 350,00 м. (12%)` и `Без категории — 2 500,00 м. (88%)` (или с категорией, если пополнение цели записалось без категории → «Без категории»).
- **Если сломано**: графики не рендерятся (пустой контейнер) ИЛИ суммы не совпадают с введёнными.

### 6. Экспорт в CSV
- Action: «Настройки» → «Скачать CSV».
- Assert: скачивается файл вида `finance-YYYY-MM-DD.csv`.
- Assert: открыв файл (в shell `cat`), видим заголовок `Дата;Тип;Сумма;Счёт;Счёт (куда);Категория;Комментарий` и ровно 3 строки данных: Зарплата 5000, Продукты 350 (Тест), Пополнение цели «Новый ноутбук» 2500.
- **Если сломано**: файл не скачивается ИЛИ структура колонок другая ИЛИ суммы не совпадают.

### 7. Перезагрузка — данные сохраняются
- Action: нажать F5 (перезагрузить страницу).
- Assert: «Общий баланс» остался `2 150,00 м.`.
- Assert: на странице «Цели» карточка «Новый ноутбук» сохранилась с прогрессом 25 %.
- **Если сломано**: после перезагрузки всё обнулилось → значит localStorage не сохраняется.

## Regression (кратко)
- Боковая навигация: все 8 пунктов кликабельны (Главная/Операции/Аналитика/Счета/Цели/Категории/Напоминания/Настройки) — помечу как «Regression».
- Категория на странице «Категории» редактируется и удаляется — не буду проверять подробно.

## Что НЕ тестирую
- Мобильный layout (user просил веб-приложение с браузера — десктоп)
- Конкурентность/многопользовательский режим (локальное приложение)
- Напоминания (daysUntil) — регрессии нет, проверю только визуально что страница открывается
- Импорт CSV/JSON — нет изменений, не в основном flow
